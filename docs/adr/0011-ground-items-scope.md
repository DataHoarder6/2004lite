# Ground items scope: full RuneLite parity minus beam/notify/hotkey

Full `grounditems` config surface ported (highlight/hide lists, price tiers,
despawn modes, tile/menu highlight); lootbeam, notify and ALT-hotkey dropped
for v1 because each needs a new facade subsystem (3D objects, notifier, key
input) with no 2004 equivalent.
